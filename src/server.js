// import express from "express";
// const app = express();
// const port = 3000;

// app.use(express.json());

// app.get("/", (req, res) => {
//   res.send("Olá, Gabriel Feijó");
// });

// app.post("/cart", (req, res) => {
//   const { id, quantity } = req.body;

//   console.log(id, quantity);

//   res.send();
// });

// app.listen(port, () => {
//   console.log(`Example app listening on port ${port}`);
// });
